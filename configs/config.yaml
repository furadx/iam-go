# Server Configuration
server:
  mode: release      # debug, release, test
  addr: :8080
  healthz: true
  middlewares: []

# Database Configuration
database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: iam
  sslmode: disable
  max_open_connections: 100
  max_idle_connections: 10
  max_lifetime: 3600  # seconds

# JWT Configuration
jwt:
  secret: change-me-in-production   # 生产环境务必改为强随机密钥
  access_expire: 900                # access token 有效期（秒），默认 15 分钟
  refresh_expire: 604800            # refresh token 有效期（秒），默认 7 天
  revoke_fail_open: true            # Redis 故障时是否放行（true=保可用，false=保安全）

# Redis Configuration（用于 JWT 吊销）
redis:
  addr: localhost:6379
  password: ""
  db: 0

# Log Configuration
log:
  level: info                    # debug, info, warn, error, fatal
  format: console                # json, console
  enable_color: true
  disable_caller: false
  disable_stacktrace: false
  output_paths:
    - stdout
  error_output_paths:
    - stderr
